<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:fitsSystemWindows="true">

    <!--
    <RelativeLayout android:layout_width="match_parent"
                    android:layout_height="match_parent">
    -->

    <include
        android:id="@+id/toolbar"
        layout="@layout/toolbar"/>

    <!-- A RecyclerView with some commonly used attributes -->
    <android.support.v7.widget.RecyclerView
        android:id="@+id/adminteam_recycler_view"
        android:scrollbars="vertical"
        android:layout_width="match_parent"
        android:layout_height="280dp"
        android:layout_below="@+id/toolbar"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:fitsSystemWindows="true" android:layout_alignParentBottom="true"/>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_anchor="@+id/adminteam_recycler_view"
        android:layout_gravity="bottom|end|right"
        app:backgroundTint="@color/md_orange_500"
        android:src="@mipmap/ic_create_white_24dp"
        android:clickable="true"
        android:layout_marginBottom="28dp" android:layout_marginRight="19dp"
        android:layout_marginTop="480dp"
        android:layout_alignParentRight="true"/>

    <!-- </RelativeLayout> -->

    <!--
    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="270dp"
        android:id="@+id/imageView"
        android:src="@drawable/about_us_image"
        android:layout_below="@+id/adminteam_recycler_view"/>
        -->

</RelativeLayout>